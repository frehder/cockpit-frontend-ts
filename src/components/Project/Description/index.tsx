import React from 'react';

const Description = (): React.ReactElement => {
    return (
        <section>
            <p>components/Project/Description</p>
        </section>
    );
};

export default Description;
