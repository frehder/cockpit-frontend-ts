import React from 'react';

const TextContent = (): React.ReactElement => {
    return (
        <section>
            <p>components/Page/TextContent</p>
        </section>
    );
};

export default TextContent;
